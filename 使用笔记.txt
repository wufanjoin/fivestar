
await Game.Scene.GetComponent<TimerComponent>().WaitAsync(5000);//等待5秒

客户端发给服务器的协议体 协议号不能低于10000 服务器内部通讯协议不要高于10000

在ILRuntime模式下要注意的

千万不要写匿名函数 各种坑

使用泛型的时候  同一类型中 泛型的名称不要定义同一名字 否则因为反射机制会识别错乱的

是用委托要提前注册

获取Attribute 只能用GetCustomAttributes方法 而且是不能通过父类获取到子类的

使用Generate CLR Binding Code来生成绑定代码
Generate CLR Binding Code by Analysis无法正常执行异步代码


ILHelper 注册委托

ILRuntimeCLRBinding 注册类型 代码生成 

System_Runtime_CompilerServices_AsyncTaskMethodBuilder_Binding 不能绑定这个类型 否则无法正常执行异步


如果项目用的是ILRuntime热更 那就一定要在模式下ILRuntime打包 因为宏的关系会导致 代码不同

注册Destroy事件 会在组件清空和 实体ID变为0 前执行 

OuterMessageDispatcher  实现消息的的处理 实现转发

InnerMessageDispatcher  内部消息的处理

Json配置表 服务ID字段用 _id 客户端用 ID 服务器不难反序列化结构体 (个人猜想和夸平台有关)


UGUI字体变色 <color=#EC6D36FF>房号</color>

Monodb数据库 值类型的字段 可以没有 但不能为:null 

安卓平台下StreamingAssets 不能用IO数据流读取

不要要Hofix项目下面使用 UNITY_EDITOR这种类型unity自带的宏 不准的因为他是热更项目

扩展协议类 里面尽量不要写字段